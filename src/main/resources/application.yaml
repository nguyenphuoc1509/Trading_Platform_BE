server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: trading-platform
  datasource:
    url: jdbc:mysql://localhost:3306/trading_platform_db
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update